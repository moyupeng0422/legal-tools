# 北大法宝 MCP 实务操作指南

> 版本：v1.0（2026-08-14）
> 依据：100+ 次真实调用逐条记录
> 用途：**给 LLM / AI Agent 直接照着调用的操作手册**。本指南只讲三件事——① 什么时候调哪个工具；② 参数怎么传才对；③ 调用后必须校验什么。

---

## 〇、核心原则（先读这四条）

1. **默认首选省积分工具**：取法条认准 ⑥ `get_law_item_content`（**25分/次**，约为 ② 的 **1/5**，且元数据更全、带锚点）。**② `get_article`（125分）功能已被⑥覆盖（贵5倍+小数截断+批复类报错），官方优化前不使用。**
2. **"内容深度"走关键词工具，"语义发现"走语义工具**：要争议焦点/双方诉辩/完整字段 → ④/⑥；不知道关键词、只有案情描述 → ⑤/①。两两组合是标准工作流。
3. **所有检索工具都没有翻页**：检索类固定 20 条（③④），语义类 size 1–20（①⑤）。拿不到 Total 之外的数据，别想着翻页凑。
4. **⚠️ 本工具家族存在多种"静默错误"**：不报错但返回错误内容（详见「三、静默错误检测清单」）。**调用后必须校验返回字段，这是防幻觉的最后一关。**

---

## 一、工具总览与选型决策树

### 1.1 十个 Server 一张表

| # | Server | 工具 | 类型 | 一句话定位 | 返回结构 |
|---|---|---|---|---|---|
| ① | pkulaw-law-search-service | `search_article` | 法规·语义 | 案情/自然语言匹配法条（错别字容错） | 裸数组 11 字段 |
| ② | pkulaw-law-search-service | `get_article` | 法规·精确 | ⛔ 功能被⑥覆盖，官方优化前不使用 | 扁平对象 11 字段 |
| ③ | pkulaw-law | `get_law_list` | 法规·关键词 | 标题/正文关键词搜法规元数据 | `{Message, Data[], Total}` |
| ④ | pkulaw-case | `get_case_list` | 案例·关键词 | **★类案研判核心**，29 字段含判决书 4 件套 | `{Message, Data[], Total}` |
| ⑤ | pkulaw-case-search-service | `search_case` | 案例·语义 | 案情描述找同类案例（可调 size） | 裸数组 13 字段 |
| ⑥ | pkulaw-fatiao | `get_law_item_content` | 法规·精确 | **★取法条默认首选**，数字条号+锚点+最新版 | `{Message, Data}` 10 字段 |
| ⑦ | pkulaw-citation-validator | `adjust_provisions` | 校验 | AI 引用法条校准（RAG 二阶段） | 数组，每项 6 字段 |
| ⑧ | pkulaw-add-doc-link | `get_linked_content` | 智能解析 | 文本条文自动加超链接（输出排版） | Markdown 文本 |
| ⑨ | pkulaw-law-recognition | `law_recognition` | 智能解析 | 文本识别法规名+溯源（校对文书） | 数组 |
| ⑩ | pkulaw-case-number-recognition | `anhao_recognition` | 智能解析 | 文本识别案号+溯源+真伪校验 | 数组，每项 9 字段 |

> 另有 AGG 聚合服务 `pkulaw-legal-search`（5 合 1），本次调研未接入；其参数/返回格式与独立版**不统一**，混用易错，建议以独立版为准。

### 1.2 选型决策树（LLM 拿到需求先走这棵树）

```text
【要法规】
  ├─ 只知法名+条号 → ⑥ get_law_item_content  ← 默认首选（25分，省 4/5 积分）
  │     └─ 查"第X条之一" → ⑥ 用小数 tiao_num（如 120.1）
  │     └─ ② get_article：功能已被⑥覆盖（贵5倍+小数截断+批复类报错），官方优化前不使用
  ├─ 知道关键词/内容/案情，不知哪部法哪条 → ① search_article（size 可调 1–20）
  └─ "确认法规存在/时效/效力/文号"的核查 → ③ get_law_list  ← 只返回法规元数据（无条文、无下钻）

【要案例】
  ├─ 要深度研判（争议焦点/双方诉辩/裁判依据）→ ④ get_case_list（29 字段）
  │     └─ 已知案号精确查 → ④ fulltext="完整案号"
  ├─ 只有案情描述，找同类案 → ⑤ search_case（size 可调 1–20）

【要校验/处理文本】
  ├─ 校验 AI 引用的法条 → ⑦ adjust_provisions
  ├─ 文本识别法规名+溯源 → ⑨ law_recognition
  ├─ 文本加法规超链（输出排版）→ ⑧ get_linked_content
  └─ 文本里提取案号+真伪校验 → ⑩ anhao_recognition
```

---

## 二、各工具操作卡（参数 + 正确用法 + 坑位）

### 工具⑥：get_law_item_content —— 取法条默认首选 ⭐

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| title | string | 是 | 法规名称，**须接近全称**，自动匹配最新版 |
| tiao_num | number | 是 | **必须 number**；整数=第X条，**小数=第X条之一**（120.1=第一百二十条之一） |

- ✅ 调用示例：`{"title": "刑法", "tiao_num": 120.1}` → 第一百二十条之一全文 + `#tiao_120.1` 锚点
- ✅ 自动取现行最新版本（"刑法"→2023修正）
- ⚠️ 坑位：
  1. `tiao_num` 传字符串被 schema 拒（`must be number`）
  2. 条号不存在 → 报 pydantic 反序列化错误（无友好提示，与"之一不存在"无法区分）
  3. **title 残缺会静默匹配跑偏且不报错**（"民法典合同编通则解释"缺"若干问题"→匹配到水利协会培训通知）
  4. **⚠️⚠️ 完整正确的 title 也可能匹配跑偏且不报错（最高风险，明确 bug）**：输入"最高人民法院关于审理劳动争议案件诉讼当事人问题的批复"（③ 中 Total=1 能命中），仍返回无关文件且**每次返回不同**
  5. 返回固定 10 字段（文档声称 14 字段已过时）

- 🔍 **调用后必须校验**：核对返回 `Title` 与请求法规是否一致；不一致 = 结果不可用，改用 ③ 查元数据。

---

### 工具②：get_article —— ⛔ 不建议使用（功能已被⑥覆盖）

> **官方优化前不要用**：功能与⑥高度重叠（都是"已知法名+条号取原文"），但积分贵 5 倍（**125分 vs ⑥的25分**）、小数条号静默截断、对批复/司法解释类标题稳定报 Internal error。**所有场景一律用⑥**。以下仅作差异备忘，供官方优化后复测。

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| title | string | 是 | 法律名称（模糊匹配可） |
| number | string | 是 | 条号，**string 高度容错** |

- ✅ `number` 四种写法均可命中同一条：`"第五百七十七条"` / `"五百七十七"` / `"577"` / `"第一百二十条之一"`
- ✅ 条号/法规不存在 → **静默空返回不报错**（判 `title==""`）
- ⚠️ 坑位：
  1. **⚠️⚠️ 小数条号静默截断（高风险）**：`number="120.1"` 不报错，悄悄解析成整数 120 → 返回"第一百二十条本体"而非"之一"；**想查"之一"必须用中文"第一百二十条之一"**
  2. **积分约为 ⑥ 的 5 倍（125分 vs 25分）**——默认优先 ⑥
  3. **⚠️⚠️ 对"批复/司法解释"类标题稳定报 Internal error**（5 种输入变体全失败，明确 bug）
  4. 元数据多为空；无条号锚点；title 模糊匹配可能静默匹配到其他文件

- 🔍 **调用后必须校验**：返回 `title` 是否等于请求；`article` 是否非空。

---

### 工具③：get_law_list —— 关键词搜法规（⚠️ 仅元数据，无条文）

> **定位提醒**：③ 只返回法规**元数据**（9字段：Title/Url/文号/机关/日期/时效/效力），**不含条文内容，也无下钻接口**。`fulltext`全文命中只告诉你"这部法规正文含这些词"，**不知道命中第几条，也拿不到条文**。
> **"关键词→条文"的正确路径是 ① search_article（语义直达法条）**。③ 只用于：确认法规存在性、时效、效力位阶、文号（核查场景），以及 ②⑥ 均失效的批复/司法解释类文件的定位。

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| title | string | 任选① | 标题关键词（连续子串包含） |
| fulltext | string | 任选① | 全文关键词（分词多词匹配） |
| start/endImplementDate | string | 否 | 日期（点分/ISO 双格式兼容） |
| timeliness | array | 否 | 时效性多选（数组内 OR，**筛选可靠**） |
| effectiveness | array | 否 | 效力位阶多选（**⚠️ 筛选有缺陷**） |

> ① title/fulltext 至少一个，服务端业务校验强制，空参报中文业务错误。

- ✅ **多词分词 AND**：空格 / 半角逗号 / 全角逗号都支持；`title="盗窃 诈骗"` = 标题同时含两词
- ✅ **最强组合**：`title="刑法" + fulltext="盗窃 诈骗" + timeliness=["现行有效"]`
- ✅ **搜法条本体要用原文表述**：`fulltext="盗窃公私财物"`（命中刑法典本体）≠ `fulltext="盗窃罪"`（只命中司法解释/地方文件——法条正文不写罪名）
- ✅ 书名号自动剥除（《民法典》=民法典）
- ⚠️ 坑位：
  1. **⚠️⚠️ effectiveness 筛选缺陷**：`effectiveness=["法律"]` 时 Total 统计生效（188→127）但**返回列表未严格过滤**，混入"地方工作文件"——须二次校验每条 `EffectivenessDic`
  2. 排序无规则且不稳定（最新版不靠前；加 timeliness 后更乱）
  3. **无布尔语法**：AND/OR/NOT 英文词仅作词元
  4. **顿号是字面字符**（"盗窃、诈骗"= 连续串匹配，非分词）
  5. 无翻页，固定返回 20 条 + Total
- 🔍 **调用后必须校验**：若用了 effectiveness 筛选，逐条核对 `EffectivenessDic`。

---

### 工具④：get_case_list —— ★类案研判核心（29 字段 + 判决书 4 件套）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| title | string | 任选① | 标题关键词（子串/空格分词 AND） |
| fulltext | string | 任选① | 全文关键词（**案号精确查目前只能用它**） |
| caseGrade | array | 否 | 数组内 OR（指导性案例/公报案例…） |
| documentAttr | array | 否 | 数组内 OR（判决书/裁定书/调解书…） |
| caseClassName | string | 否 | **string 单选**（民事一审/民事二审/首次执行） |
| court | string | 否 | **string 单选**（支持泛称子串） |
| start/endLastInstanceDate | string | 否 | 日期（点分格式） |

- ✅ **完整程序判决书**：`caseClassName="民事二审" + documentAttr=["判决书"]`（二审判决 4 件套填充率高）
- ✅ **高级别案例**：`caseGrade=["指导性案例","公报案例"]`（裁判要点有值）
- ✅ **案号精确查询**：`fulltext="(2022)鲁0191民初3255号"` → **Total=1 精确命中**（title 放案号=0）
- ⚠️ 坑位：
  1. **court/caseClassName 不能多选**（string 单值，数组被拒）；多法院分次查询合并。**court 是子串匹配**："高级人民法院"=全国 32 家高院（52,869 条）；"北京市高级人民法院"=4,478（精确）
  2. **4 件套填充率取决于数据源**：最高法/高院/二审判决大多完整；基层一审批次约 40% 完整、60% 空——筛选后须二次校验 `Identified` 非空
  3. documentAttr 实际 schema 含 6 值（含调解书），返回还可能含"其他文书"等 schema 外值——处理返回值要包容
  4. 无翻页，固定 20 条 + Total
  5. caseGrade 返回细分值（最高检/最高法指导性案例等），比对时注意
- 🔍 **调用后必须校验**：如需完整判决要素，检查 4 件套字段（Ascertain/ControversialFocus/Identified/RefereeResult）是否非空。

---

### 工具⑤：search_case —— 语义搜案例（快速发现）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| text | string | 是 | 自然语言案情/问题 |
| case_type | string | 否 | **12 枚举带"案件"后缀**（刑事案件/民事案件…） |
| doc_type | string | 否 | **5 枚举含"其他文书"** |
| courthouse_name | string | 否 | 法院全称 |
| courthouse_province | string | 否 | **省份全称（"北京市"）** |
| decision_date_start/end | string | 否 | 日期（纯 ISO） |
| size | int | 否 | **1–20，默认 10** |

- ✅ 不知道确切案由时用自然语言描述案情（"房屋租赁合同到期后房东拒绝退还押金"）→ 首条即同类纠纷
- ✅ 找刑事案例：`case_type="刑事案件"`；限省份：`courthouse_province="北京市"`
- ✅ **与 ④ 组合的标准工作流**：search_case 语义发现 → 拿案号 → get_case_list `fulltext="案号"` 精确核实（拿 4 件套）
- ⚠️ 坑位：
  1. **案号放 text 不精确匹配**（返回同法院同批次其他案件）——案号精确查必须走 ④ 的 fulltext
  2. **所有筛选参数 string 单选、无数组、无 OR**——多类型/多法院分次查询合并
  3. 无 Total；无翻页
  4. **4 件套是固定精简版 3 字段**（ascertain/identified/referee_result），**无原被告/争议焦点**——这是返回 schema 限制；需要深度要素研判必须用 ④
  5. 案由分类与内容可能不符（语义按内容匹配）；管辖裁定类 referee_result 常空
- 🔍 **调用后必须校验**：若是为研判取证，改用 ④ 重新精确查。

---

### 工具①：search_article —— 语义搜法规

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| text | string | 是 | 关键词/案情描述（语义匹配，**错别字容错**） |
| lib | string | 否 | 中央/地方（✅ 筛选可靠） |
| timeliness | string | 否 | **string 单选**（传数组被拒） |
| issue_department | string | 否 | 制定机关全称（✅ 生效） |
| implement_date_start/end | string | 否 | 日期范围 ISO（✅ 生效，疑似按 issue_date 匹配） |
| size | int | 否 | 1–20（✅ 可调） |

- ✅ **可加组合筛选**：`timeliness="现行有效" + lib="中央"`（实测可靠）
- ⚠️ 坑位：
  1. **无 Total**——无法得知全量命中数
  2. **article 字段常为空**——办法/复函/通知类命中"法规级"多于"法条级"
  3. timeliness 单选——多时效分次查询合并
  4. 无翻页；size 最大 20
  5. 同一法规的"代表条文"随查询侧重变化（不固定）

---

### 工具⑦：adjust_provisions —— 幻觉修正·法条校验（RAG 二阶段）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| userlaw | array | 任选* | 用户提到的法条 `{title, article_number}` |
| answerlaw | array | 任选* | AI 引用的法条 `{title, article_number, text}` |
| prompt | string | 否 | 用户原始问题 |

> * userlaw/answerlaw 至少一个（全空报"参数不能同时为空"）。

- ✅ **官方二阶段流程（实测验证）**：
  1. 阶段1：AI 回答时引用法条
  2. 阶段2：`answerlaw.text` 填 **AI 自己生成的法条内容（完整文字）** → 返回权威 `original_text`
  3. 对照：完全一致 → 引用原文+url+implement_date；有差异 → **用 original_text 替换**并注明"已根据北大法宝权威库校正"
  4. userlaw + answerlaw 可同时传，双向核对
- ⚠️ 坑位：
  1. **⚠️⚠️ text 严禁填"第X条"引用格式**（如"劳动合同法第47条"）——不报错但检索完全跑偏（实测返回《劳动法》97条）；**text 只填条文内容/关键词**
  2. **⚠️⚠️ "返回空数组=法条不存在"不成立**：引用不存在的条文时，工具**静默回退语义检索**返回相关条文——**必须核对返回的 `article_number` 与请求是否一致**，不一致即判定引用错误
  3. 版本去重不完整（同一条返回多版本），需自行按时效性过滤
  4. original_text 带"标题/时效性"前缀，引用前需清理
- 🔍 **调用后必须校验**：`article_number` == 请求条号？不一致 = 引用无效。

---

### 工具⑧：get_linked_content —— 法宝超链（输出排版）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| message | string | 是 | 待处理文本 |

- ✅ 文本含"《民法典》第五百七十七条" → 返回 Markdown（法规名链 chl 文档页、条号链 `#tiao_N` 锚点）
- ⚠️ 坑位：识别范围窄——仅显式条文引用（《法规名》第X条）；纯法律术语、案号/案例引用**不支持**

---

### 工具⑨：law_recognition —— 法条识别与溯源（校对文书）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| text | string | 是 | 待识别文本 |

- ✅ 识别结果用 `original`（标准名**含版本后缀**，如"刑法(2023修正)"）+ `source`（链接）做溯源
- ✅ 简称容错（"民法典"→全称）；多法规一次全识别
- ⚠️ 坑位：
  1. **fulltext 逻辑与文档相反**：**带条号 → `fulltext=""`**（不返回该条内容）；**无条号 → 回填前几条全文**（识别摘要）
  2. **错别字不识别**（"民典法"→静默不识别，不报错）——与 search_article 的语义容错形成对比
  3. 识别后取条文需另调 ⑥（或②，②已弃用）

---

### 工具⑩：anhao_recognition —— 案号识别与溯源（含真伪校验）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| text | string | 是 | 含案号的文本 |

- ✅ 完整年份案号（`(2022)鲁0191民初3255号`）→ 识别 + 完整溯源（gid/court/title/url/日期）+ **start/end 字符偏移**（可做高亮/反幻觉）
- ✅ 全角/半角括号均识别
- ✅ **gid 非空 = 库中真实存在** → 自带"案号真伪校验"能力
- ⚠️ 坑位：
  1. **⚠️⚠️ 省略年份 → 版本歧义（张冠李戴）**：`"鲁0191民初3255号"` 自动补全年份**固定补 2021**，匹配到 (2021) 浦发银行案而非已知的 (2022) 案——**案号必须带完整年份**，识别后核对 title/caseFlag
  2. 编造案号（2099 虚构）→ 格式识别但 **gid 及溯源字段全空**（空 gid=库中无此案；也可能是"存在但未收录"）
  3. **返回顺序按文本位置倒序**（靠后的案号在前），使用时应按 start 排序
  4. 无案号文本 → 返回空数组（不报错）

---

## 三、静默错误检测清单（安全红线，务必逐条执行）

> 北大法宝工具家族的共同特点：**很多错误不报错，返回错误内容让你以为是对的**。以下每条都对应一次真实踩坑。

| # | 工具 | 静默错误场景 | 检测方法 | 补救 |
|---|---|---|---|---|
| 1 | ② get_article | 小数条号静默截断（120.1→120） | 核对返回 title/条号是否含"之一" | 用中文"第一百二十条之一"或改用 ⑥ 小数 tiao_num |
| 2 | ⑥ get_law_item_content | title 无法映射时静默返回无关文件（每次不同） | **核对返回 Title 与请求一致** | 不一致则弃用，改用 ③ 查元数据定位 |
| 3 | ⑦ adjust_provisions | 不存在条文静默回退语义检索（假"校验通过"） | **核对返回 article_number == 请求** | 不一致即判定引用错误，标注为幻觉 |
| 4 | ⑩ anhao_recognition | 省略年份自动补 2021（张冠李戴） | 识别后核对 title/caseFlag 是否对应目标案 | 案号必须带完整年份；或用 ④ fulltext 复核 |
| 5 | ③ get_law_list | effectiveness 筛选 Total 生效但列表未过滤 | 逐条核对 EffectivenessDic | 手动过滤或改用 timeliness 筛选 |
| 6 | ④ get_case_list | 基层案件 4 件套 60% 为空 | 检查 Identified 是否非空 | 换二审/高院案件或改用其他数据源 |
| 7 | ⑦ adjust_provisions | text 填"第X条"引用格式检索跑偏 | 核对返回 title/条号 | text 只填条文内容/关键词 |
| 8 | ⑨ law_recognition | 错别字静默不识别 | 检查 original 是否为空 | 先用 ① 语义检索确认名称 |

**统一校验口诀**：取法条看 Title，取案例看案号/字段，校验引用看 article_number，识别类看 gid/偏移。

---

## 四、参数组合逻辑速查（实测）

| 逻辑 | 支持情况 | 位置 |
|---|---|---|
| AND | ✅ 统一 | 所有筛选参数之间、title+fulltext 跨字段 |
| OR（数组内） | ✅ | ③ timeliness/effectiveness；④ caseGrade/documentAttr |
| 空格/逗号分词 AND | ✅ | ③④ 的 title/fulltext 内多词（空格=半角逗号=全角逗号） |
| 顿号"、" | ❌ 字面字符 | 连续串匹配，非分词 |
| NOT / 字段间 OR / 括号 / 布尔词 | ❌ 全不支持 | AND/OR/NOT 英文词仅作词元 |
| 语义检索 | ✅ | ①⑤ 的 text，自然语言免拼语法 |

**条数/Total 速查**：③④ 有 Total（筛选后全量命中）；①⑤ 无 Total；全部无翻页（③④ 固定 20 条；①⑤ size 1–20）。

---

## 五、标准工作流模板（可直接套用）

### 工作流 A：法规检索 → 精确引用（法律意见/报告正文）

```text
1. ① search_article      语义定位法条（案情/自然语言，错别字容错）
2. ⑥ get_law_item_content 精确取原文（tiao_num 用 number，含"之一"用小数）
3. ⑦ adjust_provisions   校验引用（answerlaw.text 填条文内容，核对 article_number）
4. ⑧ get_linked_content  输出排版加超链
```

### 工作流 B：类案检索 → 深度研判（诉讼策略/类案报告）

```text
1. ⑤ search_case         语义发现同类案（size 开大）
2. ④ get_case_list       拿案号 fulltext 精确查（Total=1，取 29 字段 4 件套）
3. ④ caseGrade=["指导性案例","公报案例"] 取权威判例
4. ⑦ adjust_provisions   校验判决依据法条
```

### 工作流 C：文书校对 / 反幻觉（AI 生成法律内容质检）

```text
1. ⑨ law_recognition     识别文中法规名（original 含版本后缀）
2. ⑩ anhao_recognition   识别文中案号（核对 gid 非空 + title 对应 + 按 start 排序）
3. ⑥ get_law_item_content 逐条取原文比对
4. ⑦ adjust_provisions   对 AI 引用逐条校验（重点核对 article_number）
5. 输出：一致/不一致/未命中 判定 + 权威原文
```

### 工作流 D：成本敏感模式（日常快速查询，单任务≤500分）

```text
1. 取法条直接用 ⑥（25分；②已弃用别碰）
2. 优先 ③/④ 关键词检索（25分/次）而非 ①⑤ 语义（125分/次）
3. ③/④ 一次组合筛选到位（title+fulltext+timeliness），避免多次裸查
4. 能一次取多条（size=20）就不分次
5. 无 Total 的工具（①⑤）避免重复查询比对数量
6. 125 分档工具（⑦⑧⑨⑩）按需用，别默认叠加
```

---

## 六、积分成本速查（实测档位）

> 数据来源：credit-dictionary.json（2026-08-05，实测积分流水核对）。
> **档位规律一句话：`检索XX-关键词`类 = 25 分；语义 / 智能识别 / 解析 / 校验类 = 125 分。**

| 工具 | 积分/次 | 档位 | 建议 |
|---|---|---|---|
| ③ get_law_list | **25** | 关键词检索 | 法规元数据核查首选 |
| ④ get_case_list | **25** | 关键词检索 | 类案研判核心，省 |
| ⑥ get_law_item_content | **25** | 关键词检索 | **取法条默认，最省** |
| ① search_article | **125** | 语义检索 | 关键词→条文直达，贵但值 |
| ⑤ search_case | **125** | 语义检索 | 语义发现同类案，贵 |
| ② get_article | **125** | 语义解析 | ⛔ 已弃用（功能被⑥覆盖，25分/次的⑥更优） |
| ⑦ adjust_provisions | **125** | 解析/校验 | 反幻觉校验，按需用 |
| ⑧ get_linked_content | **125** | 智能解析 | 输出排版用 |
| ⑨ law_recognition | **125** | 智能识别 | 文书校对用 |
| ⑩ anhao_recognition | **125** | 智能识别 | 案号溯源用 |
| AGG 聚合版 | 禁用 | — | **一律禁用**，用独立 server 工具 |

**预算纪律（单任务）：**
- 北大法宝单任务累计 **≤500 分**（约 4 次 125 分调用），超限停下询问用户
- 常见工作流成本估算：A 法规引用 = ①(125) + ⑥(25) + ⑦(125) + ⑧(125) ≈ **400 分**；B 类案研判 = ⑤(125) + ④(25) + ④(25) + ⑦(125) ≈ **300 分**——**都在 500 分预算内，但别随意加 125 分档调用**
- 省分口诀：**能关键词（25分）不语义（125分）；能⑥不②；检索一次到位不裸查**

---

## 七、已知服务端缺陷备忘（遇到别浪费时间）

- ⑥ 对"批复/司法解释"类标题会**静默跑偏**（②更直接报错，但②已弃用）——此类文件改用 ③ 查元数据，正文取不到时标注"北大法宝暂无法取全文"
- ⑥ 官方文档"14 字段"已过时，实际固定 10 字段
- ⑤ 文档 courthouse_province 示例"北京/浙江"实需全称"北京市"
- AGG 聚合版与独立版参数/返回格式不统一，混用易错

---

*本指南应对截止2028-08-13北大法宝MCP实测情况，如后续北大法宝MCP有更新，则相关内容以实际更新为准。*
