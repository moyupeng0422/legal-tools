# 元典开放平台 API Key（hall_detect 幻觉检测等 HTTP 直调用）
# ⚠️ 已清空（2026-08-20）：原 Key 移除，防泄露；需启用 hall_detect 时重新填入
# 格式：YUANDIAN_API_KEY=sk_xxxxxxxx
# 获取：元典开放平台个人中心创建（非 MCP 的 Authorization Bearer token）
# ⚠️ hall_detect 当前为会员专属接口（非 VIP 账号返回 403，pitfall #33）——填 Key 前先确认账号 VIP 状态
YUANDIAN_API_KEY=
