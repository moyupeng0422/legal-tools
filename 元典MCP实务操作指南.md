# 元典（Yuandian）MCP 实务操作指南

> 版本：v1.0（2026-08-17）
> 依据：130+ 次真实调用逐条记录
> 用途：**给 LLM / AI Agent 直接照着调用的操作手册**。本指南只讲三件事——① 什么时候调哪个工具；② 参数怎么传才对；③ 调用后必须校验什么。
> 范围：**yuandian-law（①-⑤）+ yuandian-case（⑥-⑨）9 个法律工具**。以下类别均**不在本指南范围**（按官方接口广场分类确认）：
> - **企业信息类**（yuandian-company 企业画像工具）
> - **证券合规类**（5 个接口：证券行业法律法规语义检索接口 / 证券行业法条关键词检索接口 / 证券行业法规关键词检索接口 / 上市公司公告关键词检索接口 / 监管案例关键词检索接口）
> - **幻觉检测类**（法律幻觉校验接口——**会员专属**）
>
> **每次调用前先用 `yuandian_get_user_balance` 查余额**（元典按调用计费；⭐**余额绑定 API Key 账号**——law/case 各 Server 都配了查询工具但结果相同，**任一 Server 查一次即可**）。

---

## 〇、核心原则（先读这五条）

1. **按"检索层级"选工具（语义发现 → 关键词命中 → 编号直取 → 全文下钻）**：

| 层级 | 法规（law） | 案例（case） |
|---|---|---|
| ① 语义发现（自然语言/案情，容错） | ① `law_vector_search` | ⑥ `case_vector_search` |
| ② 关键词命中（精确过滤） | ② `rh_ft_search`（法条级）/ ③ `rh_fg_search`（法规级）——**②③侧重不同：②已定目标取条文；③先发现法规+预览命中条文段（⚠️局部有遗漏，枚举命中条文须用②）** | ⑦ `rh_qwal_search`（权威）/ ⑧ `rh_ptal_search`（普通+yyft）——**类案筛选** |
| ③ 编号直取（已知法规+法条号） | ⑤ `rh_ft_detail` | — |
| ④ 全文下钻（引用原文必走） | ④ `rh_fg_detail`（整法全文） | ⑨ `rh_case_details`（案例全文） |

> 标准链路：**语义/关键词先定位（①②⑥⑦⑧）→ 拿 id → 详情下钻（④⑤⑨）取全文**；两两组合是标准工作流。⚠️ **法条引用例外：② 的 content 已是完整条文原文（实测与⑤逐字一致），常规引用无需下钻⑤**——⑤仅用于知道法规名与具体条号后的精确确认；③→④（整法全文）、⑥⑦⑧→⑨（案例全文）才必须下钻。
2. **查"地方类/工作文件类/失效法条"必须用关键词检索②**：**语义库（①）检索范围更小**——只覆盖 10 级中央法条，7 种地方类/工作文件类层级（地方性法规/地方政府规章/地方律协规定/地方司法文件/自治条例/立法机关工作文件/行政机关工作文件）语义检索必空；**关键词库（②）检索范围更大**——全 17 级可查，`sxx="失效"` 也能查到失效法条。
3. **要"完整全文"必须走详情工具**：检索接口的 content 不保证完整——①是完整法条原文✅；⑥是 AI 提炼版（非原文）；⑦是官方发布全文（非原始文书）；⑧质量参差（约 20% 缺失或片段）。**引用原文一律下钻详情**：法条走 ⑤ `rh_ft_detail`，法规走 ④ `rh_fg_detail`，案例走 ⑨ `rh_case_details`。
4. **所有工具无翻页；仅 ⑦⑧ 有 Total**：`page` 一律静默忽略，return_num/top_k 即全部可得。别想着翻页找数据。
5. **⚠️ 本工具家族存在多种"静默错误"**：不报错但返回错误/空内容（详见「三、静默错误检测清单」）。**调用后必须校验返回字段，这是防幻觉的最后一关。**

---

## 一、工具总览与选型决策树

### 1.1 九个工具一张表

| # | Server | 工具 | 类型 | 一句话定位 | 返回结构 |
|---|---|---|---|---|---|
| ① | yuandian-law | `yuandian_law_vector_search` | 法规·语义 | 自然语言查法条（错别字容错）；⚠️仅覆盖10级中央法条 | `extra.fatiao` 16 字段 |
| ② | yuandian-law | `yuandian_rh_ft_search` | 法规·关键词 | **★法条关键词检索默认首选**，全17级+失效法条 | `data[]`（数组）18 字段 |
| ③ | yuandian-law | `yuandian_rh_fg_search` | 法规·关键词 | **⭐法规目录查询**（纯过滤枚举清单，keyword 可省，②④做不到）+ content 命中条文段预览（⚠️局部有遗漏）| `data[]`（数组）14 字段 |
| ④ | yuandian-law | `yuandian_rh_fg_detail` | 法规·详情 | 整法全文 + **⭐refer_date 历史版本**（行为时间适用） | `data` 13 字段 |
| ⑤ | yuandian-law | `yuandian_rh_ft_detail` | 法规·详情 | 单条法条全文（id 优先级最高，ftnum 三格式兼容） | `data` 16 字段 |
| ⑥ | yuandian-case | `yuandian_case_vector_search` | 案例·语义 | 口语化案情找同类案例（完整语义 query 质量极高） | `extra.wenshu` 22 字段 |
| ⑦ | yuandian-case | `yuandian_rh_qwal_search` | 案例·关键词 | 权威案例（指导性/公报/检指导等8类），**有Total** | `data.total + lst` 18 字段 |
| ⑧ | yuandian-case | `yuandian_rh_ptal_search` | 案例·关键词 | 普通裁判文书 + **⭐yyft 援引法条反查（独有能力）** | `data.total + lst` 17 字段 |
| ⑨ | yuandian-case | `yuandian_rh_case_details` | 案例·详情 | **★要案例完整全文必走**（普通深度字段/权威section） | `data[]` 列表 |

### 1.2 选型决策树（LLM 拿到需求先走这棵树）

```text
【要法规/法条】
  ├─ 只有案情/自然语言，不知哪部法哪条 → ① law_vector_search（语义）
  │     └─ 查地方类/失效法条 → 语义必空，直接改 ② rh_ft_search
  ├─ 知道关键词/法规名，要精确条文 → ② rh_ft_search（keyword+过滤；**content 即完整条文原文，可直接引用**）
  ├─ 已知具体法规名+条号 → ⑤ rh_ft_detail（fgmc+ftnum 直取，id 优先级最高）
  ├─ 法规目录查询/筛选，法规级聚合→ ③ rh_fg_search
  ├─ 要整部法规全文 / 历史版本 → ④ rh_fg_detail（fgmc + refer_date）
  ├─ 法规"存在性/时效/效力"轻量核查 → ③ rh_fg_search（紧凑元数据，比④轻）
  └─ 只知法规名要确认 → ③ rh_fg_search（fgmc 过滤，轻量确认版本/时效）

【要案例】
  ├─ 只有口语化案情 → ⑥ case_vector_search（⚠️ query 必须语义完整）
  ├─要权威案例要旨（可直接引用）→ ⑦ rh_qwal_search（source 过滤）
  ├─ 要原始裁判文书全文 → ⑧ rh_ptal_search（普通库）→ id → ⑨ rh_case_details
  ├─ 查引用某法条的判例（独有能力）→ ⑧ yyft + ft_search_mode
  └─ 已定位案例要完整详情 → ⑨ rh_case_details（id 优先）
```

---

## 二、各工具操作卡（参数 + 正确用法 + 坑位）

### 工具①：yuandian_law_vector_search —— 法条语义检索（发现）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| query | string | 是 | 自然语言查询（错别字/口语化容错）|
| rewrite_flag | boolean | 否 | 查询改写开关，默认 true。**精确查证用 false**，口语化案情用 true；不计费，可放心切换 |
| return_num | number | 否 | 默认 45（可放大超45，如 100→100 条；⚠️ 尾部噪声随条数增大）|
| fatiao_filter | object | 否 | 4 字段：`sxx`（时效）/`effect1`（效力17级）/`law_start`/`law_end`（日期 **yyyy-MM-dd**）；⚠️**额外字段静默忽略**（schema 宽松但服务端只认这4个）|

> 📋 **枚举速查（fatiao_filter）**：
> - `effect1`（效力17级）：宪法/法律/司法解释/行政法规/监察法规/部门规章/党内法规/军事法规规章/立法机关工作文件/行政机关工作文件/行业/团体规范/地方性法规/自治条例和单行条例/地方司法文件/地方政府规章/地方规范性文件/地方律协规定——⚠️**语义仅10种可命中**（宪法/法律/司法解释/行政法规/监察法规/部门规章/党内法规/军事法规规章/行业团体规范/地方规范性文件），**7种地方类/工作文件类必空→走②**
> - `sxx`（时效5种）：现行有效/失效/已被修改/部分失效/尚未生效——⚠️**语义只认"现行有效"**，其余枚举传了返回空
> - `law_start`/`law_end`：**yyyy-MM-dd**（实测 "2020-01-01"）

- ✅ 调用示例：`{"query": "试用期解除劳动合同的赔偿标准"}` → 45 条法条（含相似度 score）
- ✅ 错别字容错实测："劳动合约"仍命中劳动合同法87条（二倍赔偿）
- ✅ 可叠加过滤：`{"query": "...", "fatiao_filter": {"effect1": ["部门规章"], "sxx": ["现行有效"]}}`
- ⚠️ 坑位：
  1. **⭐ 结果混入 type=2 案例/type=3 通知**（如"第32批指导性案例"）——**必须 `filter(item.type===1)`** 才是真法条
  2. **7 种地方类/工作文件类层级语义检索必空**（数据存在但向量索引未收录）——这类 query 直接改 ②
  3. **sxx 只认"现行有效"**：`sxx=["失效"]`/`["已被修改"]` → 空返回；查失效/历史版本走 ② 或 ④⑤ 的 refer_date
  4. **fatiao_filter 额外字段被静默忽略**（schema 宽松但服务端只认 4 字段）
  5. 无 Total、无翻页
- 🔍 **调用后必须校验**：逐条 `filter(item.type===1)`；查地方类结果为空时改用 ②。

---

### 工具②：yuandian_rh_ft_search —— 法条关键词检索（★默认首选）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| keyword | string | 是 | 关键词（**空参→501"keyword 参数不可为空！"**）|
| search_mode | string | 否 | `AND`（默认）/`OR`——空格拆分拼接模式 |
| fgmc | string | 否 | 法规名（**空格=AND 全命中**；不支持 AND/OR 关键字；不受 search_mode 影响）|
| xljb_1 | string | 否 | 效力级别 17 级（空格拆分命中任一=OR）|
| sxx | string | 否 | 时效（**全时效可用**；`sxx="失效"` 可查失效法条）|
| dy / fbbm | string | 否 | 地域 / 发布部门（空格拆分 OR）|
| fbrq/ssrq_start/end | string | 否 | 发布日期 / 实施日期范围 |
| top_k | number | 否 | 默认 10（**无 max50 硬上限**，51→51 条；0→回退 10；**建议≤20** 防响应体爆炸）|

> 📋 **枚举速查（②③ 通用过滤字段）**：
> - `xljb_1`（效力17级）：宪法/法律/司法解释/行政法规/监察法规/部门规章/党内法规/军事法规规章/立法机关工作文件/行政机关工作文件/行业/团体规范/地方性法规/自治条例和单行条例/地方司法文件/地方政府规章/地方规范性文件/地方律协规定——**关键词库全17级可查**（vs 语义①仅10级）
> - `sxx`（时效5种）：现行有效/失效/已被修改/部分失效/尚未生效——**全时效可查**（`sxx="失效"` 可查失效法条，语义①查不到）
> - `dy`（地域32个，**含"中央"**）：中央/北京/天津/河北/山西/内蒙古/辽宁/吉林/黑龙江/上海/江苏/浙江/安徽/福建/江西/山东/河南/湖北/湖南/广东/广西/海南/重庆/四川/贵州/云南/西藏/陕西/甘肃/青海/宁夏/新疆——⚠️**与⑥⑦的 xzqh_p 枚举体系不同**（xzqh_p 无"中央"、多"最高/新疆生产建设兵团"）
> - `search_mode`：`AND`（默认）/`OR`
> - `fbrq`/`ssrq` 日期：**yyyy-MM-dd**
> - ⚠️ **实测与文档不一致**：`top_k` 文档"max50"，**实测 51→51 条（无硬上限）**、0→回退10

- ✅ **查地方类/工作文件类/失效法条 → 必须用它**（语义①这些场景必空）
- ✅ 多词组合：`{"keyword": "专利 侵权", "search_mode": "AND"}` = 两词同命中；`"OR"` = 任一命中
- ✅ 多维过滤：`{"keyword": "专利", "fgmc": "专利法", "xljb_1": "法律", "sxx": "现行有效"}`（跨字段 AND）
- ⚠️ 坑位：
  1. **⭐ fgmc 语法与 sxx/dy/fbbm 不统一**：fgmc 空格=AND（全命中）、sxx/dy/fbbm 空格=OR（命中任一）——同套参数两套规则，最易混淆
  2. **top_k 文档"max50"与服务端不符**（51→51 条软上限）——大 top_k 响应体巨大
  3. 无 type 字段（无法快速过滤法条/议案报告/批复类"非典型条文"，靠 fgmc+xljb_1 判断）
  4. 无 Total、无翻页
- 🔍 **调用后必须校验**：`ft_num` 是否含"第X条"（区分真法条 vs 章节序号）；查法规名用 fgmc 而非 keyword。

---

### 工具③：yuandian_rh_fg_search —— 法规目录查询（发现/筛选，keyword 可省）

> **⭐定位：元典的"法规目录查询器"——查法规清单**。它**不强制给关键词**（②必须给）、**也不强制给法规名**（④必须给）——只给过滤条件（效力/地域/时效/发布部门/日期）就能列出一批符合条件的法规。**一句话记分工：②搜条文、③查清单、④取全文**——想"先看看有哪些法规"时就用③。

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| keyword | string | 否 | **选填**（⚠️ 但请求体不能为空，{}→500"请求参数不能为空"）|
| 其余 | — | 否 | 与②完全一致（search_mode/fgmc/xljb_1/sxx/dy/fbbm/日期/top_k）——**枚举同②**（📋见工具②枚举速查）|

- ✅ **⭐纯过滤目录模式（独有能力）**：不传 keyword，`{"xljb_1": "地方性法规", "dy": "广东", "sxx": "现行有效"}` → **枚举广东现行有效地方性法规清单**（实测：云浮排水条例/河源扬尘条例等5部，每条含 fbbm/ssrq/fwzh 文号/xljb_2 等完整元数据）——**"法规目录浏览/筛选"只有③能做**（② keyword 必填；④必须知道名称）
- ✅ **主题→法规聚合**：传 keyword 时按**法规维度**聚合（一部法规一条），vs ②按条文维度（散落多条）——"先看哪些法规涉及该主题，再决定下钻哪部"
- ✅ **轻量元数据核查**：法规存在性/时效/效力/文号/版本核查用③（紧凑元数据、一次多条），比④（单部全文，重）省流量
- ✅ **⭐content=命中条文段**：传 keyword 时，content 展示**命中点附近的一段连续条文**——**含中文条号 + 章节标题 + 【条旨】 + 条文全文**（如劳动合同法命中"解除"→ content 展示 34-40 条）——**可预览命中法规的涉事条文，比②的纯元数据更有信息量**；短文件（办法/复函类）content 接近全文；复函/通知类（无条文结构）为自然段落无条号
- ✅ 列表结果拿 `id` → 下钻 ④ `rh_fg_detail` 取全文
- ⚠️ 坑位：
  1. **keyword 是全文匹配**：content 命中关键词即返回整部法规（实测"知识产权"命中任免条例——因其含"北京知识产权法院"字样）——**查特定法规名必须用 fgmc，别用 keyword**
  2. **⭐content 是"局部条文段"且会遗漏命中条文**：⚠️⚠️ **不是所有命中条文都展示**——实测 fgmc="劳动合同法"+keyword="解除"，工具②命中条文分布在 2/21/36/37/38/39/42/43/46/48/50/65/87/89 条，但工具③ content 只展示 34-40 条（且 34/35 条不含"解除"）——**大量命中条文被遗漏**；content 只是"命中点附近的一段"，**不能用于枚举某法规的所有命中条文**（穷尽命中条文必须用工具②，法条级逐条带 ft_num）
  3. **无 ft_num 字段**：返回中无法程序化定位"命中哪几条"——**精确定位/引用单条必须走 ②→⑤**（拿 ft_num）或 ⑤ fgmc+ftnum 直取；确认完整条文走 ④
  4. 空请求体报 500（与②的 501 错误码不同，勿混淆）
  5. top_k 51→51 软上限、0→回退10；无 Total、无翻页
- 🔍 **调用后必须校验**：确认返回 title 是法规级（无 ft_num 条号）而非法条；⚠️ **预览判断相关≠穷尽命中**——如需枚举所有命中条文改用 ②；引用条文从 content 条号人工确认命中位置，再走 ⑤ 取精确条文。

---

### 工具④：yuandian_rh_fg_detail —— 法规详情（★历史版本核心）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| id | string | 任选① | 法规ID（与 fgmc 等价）|
| fgmc | string | 任选① | 法规名（**连续子串匹配**：全名/简称"劳动合同法"均可；不支持空格/AND/OR）|
| refer_date | string | 否 | 参考日期（**yyyy-MM-dd**）→ **返回该日期"当时有效"的版本**（检索定位后、确认行为时点版本内容用）；✅ 整法级返回全文，按【条旨】定位条文，无条号错位风险 |

> ① id/fgmc 至少传一个。

- ✅ **历史版本实测**：`{"fgmc": "中华人民共和国劳动合同法", "refer_date": "2010-01-01"}` → 返回 **2007 原版**（主席令65号/sxx=已被修改/第57条"注册资本五十万"）——**检索定位后、确认行为时点版本必用它**
- ✅ 版本识别看 6 字段：fgid / fwzh（主席令号）/ sxx / fbrq / ssrq / fgmc（版本后缀）
- ⚠️ 坑位：
  1. **⭐ fgmc 版本后缀陷阱**：`fgmc="中华人民共和国劳动合同法(2012修正)"`（带后缀）→ **误匹配 2007 旧版**！——**查版本三原则：①查现行=通用名不带后缀 ②查历史=通用名+refer_date ③绝不要传版本后缀**
  2. **⭐不存在法规→静默空返回**（code=200+"未查询到相关内容"，无 data）——**返回 200 不等于查到了**：`data` 缺失且 message 为"未查询到相关内容"= 本次没查到（等同其他系统的 404；可能是参数拼错或真不存在，形态相同无法区分）——**必须检查 message，不能只看 code**（只看 200 会误判"查询成功"→基于空结果下错误结论）
  3. `type="法规"` 是字符串常量（与①的 type 数字 1/2/3 语义不同）
- 🔍 **调用后必须校验**：核对返回 `fwzh`/`sxx`/`fgmc` 是否为目标版本（尤其用 fgmc 查询时）。

---

### 工具⑤：yuandian_rh_ft_detail —— 法条详情（最细粒度）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| id | string | 任选① | **⭐id 优先级最高**（全字段同时传时只按 id 返回）；id=fgid_条号，与②检索 id 无缝衔接 |
| fgmc + ftnum | string | 任选① | fgmc 同④（连续子串）；ftnum 三格式兼容（见下）|
| refer_date | string | 否 | 法条级历史版本（与④同机制，**yyyy-MM-dd**）；⚠️ **须已知目标时点的确切条号**（条号可能随版本偏移）|

> ① id 或 fgmc+ftnum 至少传一个。

- ✅ **ftnum 三格式**：中文"第八十七条" / 数字"87" / **带小数点"133.1"=第一百三十三条之一**（内部机制：元典用 tid="133.1" 表示"之一"）——三种全兼容
- ✅ 查历史版本：`{"fgmc": "中华人民共和国劳动合同法", "ftnum": "第五十七条", "refer_date": "2010-01-01"}` → 2007 版第57条（⚠️本例两版57条恰为同主题条文，属"侥幸"）
- ⚠️ 坑位：
  1. **⭐"之一"不存在时前缀回退母条且不报错**：`ftnum="第八十七条之一"`（劳动合同法无此条）→ **返回第八十七条**——**必须核对返回的 ft_num**
  2. **⭐ refer_date 条号错位陷阱**：不同版本条号**未必一一对应**（大修/重编法规尤甚，如2023公司法218→284条）——**拿现行版条号套历史版，可能命中"同名不同内容"的无关条文**！**优先用④整法级**（按【条旨】定位，无此问题）；确需⑤则先确认历史版确切条号
  3. fgmc 版本后缀陷阱同④（带"(2012修正)"误匹配旧版）
  4. 校验规则（get_api_doc 文档确认）：**空请求体 → 500**"请求参数不能为空"；**id 为空且 fgmc+ftnum 同时为空 → 501**（id 为空时 fgmc+ftnum 必填）；不存在（fgmc/条号/id 任一）→**静默空返回**（code=200+message"未查询到相关内容"）——**200≠查到**，须检查 message
  5. `_score` 恒=1.0（详情接口占位）；`type="法条"` 字符串（①数字type/④"法规"/⑤"法条"三处全不同）
- 🔍 **调用后必须校验**：返回 `ft_num` == 请求条号？不一致 = 前缀回退误命中，弃用。

---

### 工具⑥：yuandian_case_vector_search —— 案例语义检索（发现）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| query | string | 是 | 自然语言案情（口语化容错）|
| return_num | number | 否 | 默认 45（可放大超45，如 100→100 条；⚠️ 尾部噪声随条数增大）|
| wenshu_filter | object | 否 | 11 维：wenshu_type/ay/wszl/ja_start/ja_end/dianxing/fayuan/source/cj/xzqh_p/xzqh_c |

> 📋 **枚举速查（wenshu_filter，文档 get_api_doc 确认）**：
> - `wenshu_type`（案件类别10种）：刑事案件/民事案件/行政案件/执行案件/管辖案件/国家赔偿与司法救助案件/强制清算与破产案件/国际司法协助案件/非诉保全审查案件/其他案件
> - `ay`：**完整案由名称**（数组OR），如"侵害发明专利权纠纷"（非编码）
> - `wszl`：**编码**（数组OR）——1=判决书/2=裁定书/3=调解书/4=决定书/5=通知书/6=支付令/7=申请书/8=起诉书/9=抗诉书/10=起诉状/11=上诉状（⚠️传名称→静默空返回）
> - `source`（8种，**仅权威库生效须配 dianxing=true**）：典型案例/参考案例/公报案例/解纷案例/参阅案例/刑事参考案例/指导性案例/检指导案例
> - `cj`（法院层级4种）：基层/中级/高级/最高
> - `xzqh_p`（省份**33个** = 31省+最高+新疆生产建设兵团）：北京/天津/…/新疆/**最高**/**新疆生产建设兵团**（⚠️无"中央"，与②③ dy 不同）
> - `ja_start`/`ja_end`：**yyyy-MM-dd**

- ✅ **⭐ query 必须语义完整**（决定性因素）：短语"专利侵权"45条噪声 11%；完整语义"侵犯专利权赔偿数额如何计算"45条 0 噪声——**最佳返回条数=默认45条（query 语义完整前提下）**
- ✅ 口语化容错："房租到期房东不退押金怎么办"→全部命中房屋租赁纠纷
- ✅ 查权威来源：`{"query": "...", "wenshu_filter": {"dianxing": true, "source": ["指导性案例"]}}`
- ⚠️ 坑位：
  1. **⭐ wszl 只认编码"1"-"11"**（1=判决书…11=上诉状）：传名称"判决书"→**静默空返回{}**！（编码表在 get_api_doc）——**与⑦⑧的 wszl 传名称完全相反**
  2. **source 仅对权威库生效**：必须配 `dianxing=true`，否则普通库照常返回（筛选无效）
  3. **content=AI 提炼的"整理后内容"（非原文）**——引用原文须走 ⑨
  4. 大 return_num 尾部 score 暴跌（100 条尾部 0.08）、噪声混入——**扩量过滤阈值：严格 ≥0.99 / 宽松 ≥0.65（实测相关下限）**，优先改 ⑦⑧ 关键词检索
  5. `type` 字段仅普通库有（1/2），权威库无；区分库看 `db` 字段 + url（case/ vs qwcase/）
  6. jaDate=0/jand=-1=日期缺失；无 Total、无翻页；**服务端无截断**（长响应为工具层持久化到文件，读取即全量）
- 🔍 **调用后必须校验**：短 content 案例用五维信号判断相关性——**score（实测锚点：严格 ≥0.99 / 宽松 ≥0.65）** + title + anyou 案由 + wszl（裁定书天然短）+ 下钻 ⑨ 验证。

---

### 工具⑦：yuandian_rh_qwal_search —— 权威案例检索（精查）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| qw | string | 否 | 全文关键词（空格+search_mode）|
| search_mode | string | 否 | and/or（默认 and；**qw 支持 OR**；⚠️ 传非法值 → **500**"search_mode 不合法"）|
| ah / title | string | 否 | 案号（**仅权威案号可见**）/ 标题（**空格=AND**）|
| ay / jbdw / source / xzqh_p / wszl | **数组** | 否 | **数组内 OR**；wszl 传**名称**（判决书/裁定书/调解书/决定书）|
| ajlb | string | 否 | 案件类别（单值非数组）|
| ja_start/ja_end | string | 否 | 裁判日期范围 |
| top_k | number | 否 | 默认10；**⭐max50 硬上限**（51→50 截断）|

> 📋 **枚举速查（⑦⑧，文档 get_api_doc 确认）**：
> - `source`（8种）：典型案例/参考案例/公报案例/解纷案例/参阅案例/刑事参考案例/指导性案例/检指导案例
> - `wszl`（4种，**传名称**，数组OR）：判决书/裁定书/调解书/决定书——⚠️**与⑥的编码相反**（整套元典最易踩坑）
> - `ajlb`（案件类别10种）：刑事案件/民事案件/行政案件/执行案件/管辖案件/国家赔偿与司法救助案件/强制清算与破产案件/国际司法协助案件/非诉保全审查案件/其他案件
> - `xzqh_p`（省份**33个** = 31省 + **最高** + **新疆生产建设兵团**）（⚠️无"中央"，与②③ dy 不同）
> - `search_mode`：`and`（默认）/`or`——⚠️**传非法值→500"search_mode 不合法"**（文档校验规则）
> - `ja_start`/`ja_end`：**yyyy-MM-dd**
> - ⑧ 独有：`yyft`=法条**全名+第X条**格式（如"中华人民共和国专利法第六十五条"）；`ft_search_mode`=and/or
> - ✅ 实测与文档**一致**：`top_k` max50 硬上限（51→50截断）、<=0回退10（vs ②③ 软上限是实测差异点）

- ✅ **⭐有 Total**（data.total）——与①-⑥"无 Total"不同，**可用 total 感知全量命中**
- ✅ 查权威案例要旨：`{"source": ["指导性案例"], "ay": ["侵害发明专利权纠纷"]}`（跨字段 AND + 数组内 OR）
- ✅ **content=官方权威案例全文，引用裁判要旨/要点可直接用**（指导性=关键词+裁判要点+基本案情+结果+理由；公报=裁判摘要+案件事实+裁判分析+判决结果；检指导=要旨+案情+分析+指导意义；**参考案例=裁判要旨+案情+分析+结果**）
- ⚠️ 坑位：
  1. **⭐ wszl 传名称可用**（vs ⑥传编码）——同名字段两种用法，整套元典最易踩坑
  2. **仅权威库**：普通案号（（2019）最高法知民终152号）查不到 → 须走 ⑧ ptal
  3. **jbdw=法院维度过滤**（fbdw 发布单位 或 经办法院 任一命中）——传具体法院名（如"河北省保定市中级人民法院"）可按经办法院精确过滤；传"最高人民法院"会命中所有最高法发布案例
  4. ⚠️ **个别案例源数据截断**（指导案例45号 content 停在"www.job"半截话）——**引用长文前抽查结尾**
  5. 无案号案例 llm_content 前缀="null##"
- 🔍 **调用后必须校验**：引用长文前检查 content 结尾完整性；要原始文书全文改走 ⑧/⑨。

---

### 工具⑧：yuandian_rh_ptal_search —— 普通裁判案例检索（★yyft 独有能力）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| qw / search_mode / ah / title / ay / jbdw / xzqh_p / wszl / ajlb / 日期 / top_k | — | 否 | 与⑦一致（数组内 OR；wszl 传名称；top_k max50 硬上限；⚠️ search_mode 非法值→500）|
| **ssqy** | string | 否 | **涉诉企业**（企业当事人名称子串匹配）——华为→36件 |
| **fxgc** | string | 否 | **分析过程关键词**（找特定抗辩/说理要点）——"现有技术抗辩"→1989件 |
| **yyft** | **数组** | 否 | **⭐援引法条反查=独有能力**：`["中华人民共和国专利法第六十五条"]` → **total=24671 件引用案例**|
| **ft_search_mode** | string | 否 | and/or 控制 yyft 多法条拼接：双法条 **and=5 vs or=24702** |

> 📋 枚举同⑦（source/wszl/ajlb/xzqh_p/日期，见工具⑦枚举速查）；⑧ 独有字段 `ssqy`/`fxgc` 为自由文本（子串/关键词），`yyft` 须用"法规全名+第X条"格式。

- ✅ **查引用某法条的判例（独有能力）**：`{"yyft": ["中华人民共和国专利法第六十五条"], "ft_search_mode": "and"}` → 同时引用多条的案例（更精准）
- ✅ 查企业涉诉：`{"qw": "专利侵权", "ssqy": "华为"}`；查特定抗辩：`{"qw": "...", "fxgc": "现有技术抗辩"}`
- ✅ **⭐有 Total**（同条件 32279 vs qwal 629——普通库远大于权威库）
- ⚠️ 坑位：
  1. **⭐ content 质量参差（实测 50 条）**：~80% 完整原始文书（含书记员落款），但部分仅文书开头片段（最短206字符）、**部分 content=null 缺失（6%，调解书/撤诉裁定，llm_content 只有"案号##null"）**——**要保证完整全文必须走 ⑨**
  2. **ay 缺失率 40%**（执行/非诉/催告类文书）
  3. **⭐ ah 字段可能被案由名填充**：无案号的撤诉裁定 ah="商标权权属、侵权纠纷"——**ah 值不总是真实案号，需配合 title 判断**
  4. **cj 字段偶发异常值"4"**：正常枚举=**基层/中级/高级/最高**——"4"=源数据编码未映射的偶发质量问题，判断层级前仍建议先校验枚举
  5. yyft 检索时 score=0.0（无相似度评分）
- 🔍 **调用后必须校验**：要引用原文 → 走 ⑨ 取完整全文（同案例实证：检索 206 字符片段 vs 详情完整全文）；核对 ah/title 对应真实案件。

---

### 工具⑨：yuandian_rh_case_details —— 案例详情（★要完整全文必走）

| 参数 | 类型 | 必填 | 要点 |
|---|---|---|---|
| id | string | 任选① | 案例标识；**⭐id 优先级高于 ah**（同时传返回 id 对应案例）|
| ah | string | 任选① | 案号；**可返回多条**（同案号权威库多来源）|
| type | string | 否 | `ptal`（普通）/ `qwal`（权威）限定库 |

> ① id/ah 至少传一个；返回最多 10 条（ah 实测 2 条）；**不存在→静默空返回**（code=200+"未查询到相关内容"）——**200≠查到**，须检查 message。

- ✅ **取案例完整全文首选**：同一案例详情 content 完整（普通含当事人/审判长/书记员落款；权威含官方【】分节全文）——**检索接口片段 vs 详情完整全文的实证闭环**
- ✅ 检索下钻：⑥ 拿 scid / ⑦⑧ 拿 id → ⑨ 详情
- ⚠️ 坑位：
  1. **普通 vs 权威字段体系完全不同**：
     - **普通案例**：dsr（当事人）/pjjg（判决结果）/ssjl（审理经过）/yyft（援引法条数组）/ajlx/judge（法官数组）/lawyer（律师数组）/fxgc/ww
     - **权威案例**：**section 分节数组**（关键词/裁判要点/相关法条/基本案情/裁判结果/裁判理由）+ fbdw/cprq；**judge/lawyer=[] 空数组**
     ——代码处理须区分两套结构
  2. 无效案号/id → 静默空返回（code 仍 200）——须判断 message/data
  3. url 双路径：普通=caseDetail/case/、权威=caseDetail/qwcase/（可作库类型信号）
- 🔍 **调用后必须校验**：按 type 字段分支处理普通/权威字段；data 为空时检查 message 是否"未查询到相关内容"。

---

## 三、静默错误检测清单（安全红线，务必逐条执行）

> 元典工具家族的共同特点：**很多错误不报错，返回空内容或错误内容让你以为是对的**。以下每条都对应一次真实踩坑。

| # | 工具 | 静默错误场景 | 检测方法 | 补救 |
|---|---|---|---|---|
| 1 | ① law_vector_search | 结果混入 type=2/3 案例/通知 | 检查 type 字段 | `filter(item.type===1)` |
| 2 | ① law_vector_search | `sxx=["失效"]`/`["已被修改"]`→空返回 | extra 为空 | 语义只索引现行有效，查失效走② |
| 3 | ① law_vector_search | 7 种地方类层级 query→空返回 | extra 为空 | 数据存在，改②关键词检索 |
| 4 | ④⑤ fg_detail/ft_detail | fgmc 带版本后缀→**静默返回旧版** | 核对 fwzh/sxx/fgmc 版本 | 通用名不带后缀；查历史用 refer_date |
| 5 | ⑤ ft_detail | "之一"条号不存在→**前缀回退母条** | 核对返回 ft_num | 不一致即弃用，改用正确条号 |
| 6 | ⑥ case_vector_search | wszl 传**名称**→静默空返回（只认编码1-11）| 返回 extra 是否为空 | wszl 必须传编码（get_api_doc 查表）|
| 7 | ⑥ case_vector_search | source 不配 dianxing=true→**筛选静默无效** | 返回是否含普通库案例 | 必须 `dianxing:true + source` |
| 8 | ⑦ qwal | jbdw 传"最高人民法院"→**命中 fbdw 发布单位维度**（jbdw=**fbdw 或经办法院任一命中**，非"而非"；返回条目经办法院可能是各级法院）| 核对返回条目 jbdw 字段是否含目标法院 | 按经办法院过滤须传具体法院名（如"河北省保定市中级人民法院"）|
| 9 | ⑦⑧ qwal/ptal | wszl 传**编码**→无结果（只认名称）| 返回条数/Total 是否为 0 | wszl 传名称"判决书"——**与⑥相反** |
| 10 | ⑧ ptal | 调解书/撤诉裁定 content=null 缺失 | content 字段是否存在 | 要全文走⑨详情 |
| 11 | ⑧ ptal | ah 字段被案由名填充（无案号文书）| ah 是否含"（年份）"格式 | 配合 title 判断真实案件 |
| 12 | 全部（**②③文案不同**）| 不存在（法规/条号/案例/案号/id）→**code=200 静默空返回**——⚠️空结果文案因工具而异：**②③="未查询到相关数据"**；④⑤⑦⑧⑨="未查询到相关内容" | 检查 message + data 是否缺失 | **不能只看 code**，须判断 message/data |

**统一校验口诀**：取法条看 type/条号，取版本看 fwzh/sxx，取案例看 ah/title/content 完整性，任何"空返回"都要看 message。

---

## 四、参数组合逻辑速查（实测）

| 逻辑 | 支持情况 | 位置 |
|---|---|---|
| AND | ✅ 统一 | 所有筛选参数之间（跨字段 AND）|
| OR（数组内） | ✅ 统一 | **所有数组参数**：ay/jbdw/source/wszl/xzqh_p/fayuan/yyft——多值命中任一 |
| OR（显式） | ✅ 元典特色 | `search_mode=or`（keyword/qw 空格拆分）；`ft_search_mode=or`（yyft 多法条）——**区别于北法宝"无布尔"** |
| 空格拆分 | ✅ 统一 | keyword/qw 空格 + search_mode 控制；**fgmc/title 空格=AND**（全命中）；**sxx/dy/fbbm/xljb_1 空格=OR**（命中任一）|
| AND/OR 关键字 | ❌ | fgmc 中当字面词元（实测空返回）；title 未实测布尔关键字 |
| NOT / 括号 | ❌（据机制推断）| — |
| 语义检索 | ✅ | ①⑥ 的 query，自然语言免拼语法 |

**条数/Total 速查**：⑦⑧ 有 Total（筛选后全量命中）；①-⑥ 无 Total；**全部无翻页**（page 静默忽略）；return_num（①⑥ 默认45）/top_k（②③ 默认10；⑦⑧ max50 硬上限）即全部可得。

**⚠️ 同一参数名跨工具差异速查（最易踩坑）**：

| 参数 | ⑥ case_vector_search | ⑦ qwal / ⑧ ptal |
|---|---|---|
| wszl | **传编码**（"1"-"11"）| **传名称**（"判决书"）|
| source | 仅对权威库生效（须 dianxing=true）| 直接生效 |
| jbdw | 返回字段（经办法院）| 过滤参数（法院维度，fbdw/经办法院任一命中）|

**⚠️ 文档 vs 实测对照速查（含文档未提及的实测补充/边界，个别与文档不一致；LLM 别照文档字面执行）**：

| # | 字段/行为 | 文档（schema/get_api_doc）说 | 实测结论 | 影响 |
|---|---|---|---|---|
| 1 | ① `effect1` | 17级枚举 | **语义仅10级可命中**，7种地方类/工作文件类必空 | 查地方类直接走② |
| 2 | ① `sxx` | 时效枚举（5种）| **语义只认"现行有效"**，其余空返回 | 查失效/历史走②或④⑤ refer_date |
| 3 | ① `fatiao_filter` | object（additionalProperties true）| **服务端只认 sxx/effect1/law_start/law_end 4字段**，额外字段静默忽略 | 别传 type 等做服务端过滤 |
| 4 | ① 返回 `type` | 法条级数字 1/2/3 | 混入 type=2 案例/type=3 通知 | 必须 `filter(item.type===1)` |
| 5 | ②③ `top_k` | "最大 50" | **51→51 条，无硬上限**；0→回退10 | 别按 50 截断信任结果条数；建议 ≤20 防响应体爆炸 |
| 6 | ②③ `dy` vs ⑥⑦ `xzqh_p` | 各自枚举 | **枚举体系不同**：dy=32个（**中央**+31省）；xzqh_p=33个（31省+**最高**+新疆生产建设兵团）| 别混用两套枚举 |
| 7 | ④⑤⑦⑧ 不存在目标 | — | **静默空返回**（code=200 + message"未查询到相关内容"）| 必须判断 message/data，不能只看 code |
| 8 | ①⑥ `return_num` | 默认45 | **①⑥均实测可超45**；⚠️尾部 score 暴跌噪声混入 | 默认45纯度高；扩量须 score 过滤（①还须 type=1 过滤混入的案例/通知）|
| 9 | ⑥ `source` | "仅对权威案例生效" | **须配 `dianxing=true`**，否则普通库照常返回 | source 必须与 dianxing 组合 |
| 10 | ⑥ `wszl` | 编码表（1-11）| 传**名称**→静默空返回{}（不报错）| 只能传编码 |
| 11 | ⑦⑧ `wszl` | 名称枚举（4种）| 传**编码**→无结果 | 只能传名称（与⑥相反）|
| 12 | ⑦⑧ `search_mode` | and/or | 非法值→**500"search_mode 不合法"**（500 而非静默）| 传值前校验 |

---

## 五、标准工作流模板（可直接套用）

### 工作流 A：法规检索 → 精确引用（按场景分支，选最短路径）

```text
【A1：已知法规名+条号 → 直接取文】
  → ⑤ rh_ft_detail（fgmc+ftnum 直取，id 优先级最高）；要历史版本再 + refer_date

【A2：只有自然语言/案情 → 从零定位】
1. ① law_vector_search   语义定位（自然语言，错别字容错）→ ⚠️ filter(item.type===1) 剔除混入的案例/通知
   └─ ①查地方类/失效法条必空 → 直接改 ②
2. ② rh_ft_search        精确核实（keyword+fgmc 过滤；**content 即条文原文，可直接引用**）
3. （可选）⑤ rh_ft_detail  仅需历史版本（refer_date）时下钻；常规引用第2步已够

【A3：只想知道"哪些法规涉及该主题" → 法规清单】
  → ③ rh_fg_search（keyword 法规级聚合，一部法规一条）
  → 决定下钻哪部 → ② 取条文 / ④ 取整法全文

【A4：要整部法规全文 / 历史版本】
  → ④ rh_fg_detail（fgmc + refer_date，整法级按【条旨】定位，无条号错位风险）
```

### 工作流 B：类案检索 → 深度研判（按场景分支，选最短路径）

```text
【B1：已知案号/案例id → 直接取详情】
  → ⑨ rh_case_details（id 优先于 ah；ah 可返回多条；type 限定库）

【B2：只有口语化案情 → 从零发现】
1. ⑥ case_vector_search   语义发现（完整语义 query，默认45条，0噪声）→ 短 content 用五维信号（score≥0.99/anyou/title）判断相关
2. ⑦ rh_qwal_search       权威案例精查（source 过滤；**content=官方要旨，可直接引用**）
3. ⑧ rh_ptal_search       普通库原文检索（ssqy 涉诉企业 / fxgc 抗辩要点；content 多数完整）
4. （可选）⑨ rh_case_details  仅当需完整全文时下钻：⑦遇源数据截断、⑧ content 缺失/仅片段、或要引用原文（常规研判第2-3步已够）

【B3：要权威案例要旨（可直接引用，不追原始文书）】
  → ⑦ rh_qwal_search（source=["指导性案例","公报案例"]；content=裁判要点/要旨）

【B4：援引法条反查（元典独有能力，其他 MCP 做不到）】
1. ⑧ rh_ptal_search：{"yyft": ["中华人民共和国专利法第六十五条"], "ft_search_mode": "and"} → 引用该条的判例 total
2. 追加过滤：xzqh_p/日期/ssqy 缩小范围
3. 拿 id → ⑨ 详情核对"本院认为"援引原文
```

### 工作流 C：引用校验（法条/判例）

> ⚠️ 法律幻觉校验接口=**会员专属**——本工作流用 ⑤⑧⑨ **手工实现同等校验**。

```text
【C1：校验 AI/文书中的法条引用（最常用）】
1. 取引用的 法规名+条号 → ⑤ rh_ft_detail（fgmc+ftnum 直取）→ 拿权威原文 content
2. 对照：引用内容 == 权威原文 → 引用有效；有差异 → **用权威原文替换**并注明"已按元典权威库校正"
3. ⚠️ 校验两个陷阱：返回 ft_num == 请求条号？（"之一"不存在会前缀回退母条）；版本是否正确（fgmc 不带后缀，历史版本 + refer_date）

【C2：校验判例引用真实性】
1. 案号 → ⑨ rh_case_details → 确认案例真实存在、取完整全文核对
2. 疑"某判例是否真援引某法条" → ⑧ yyft 反查（total>0 即引用过）

【C3：校验版本/时效适用性】
  → ④⑤ refer_date 按行为时间确认版本（整法级④优先，无条号错位风险）；sxx 查时效
```

### 工作流 D：成本敏感模式（日常快速查询）

```text
1. 调用前先查余额 yuandian_get_user_balance（任一 Server 即可，余额共享）
2. 查法规/法条直接用 ② 关键词检索（一次组合筛选到位：keyword+fgmc+xljb_1+sxx）
3. 语义①⑥ 仅在"不知道关键词、只有自然语言"时用；query 务必语义完整（默认45条直接用，纯度高）
4. 要全文才下钻详情（④⑤⑨），别在检索接口纠结 content
5. 无 Total 的工具避免重复查询比对数量
```

---

## 六、积分成本速查（实测档位）

> 数据来源：`get_api_doc` 逐工具实测（2026-08-17，**9/9 全查**，price 字段）。
> **档位规律一句话：语义检索 = 15 分；关键词检索 / 法规详情 = 5 分；案例详情 = 10 分；法条详情 = 1 分。**
> ⭐**余额绑定 API Key 账号**（law/case 共享同一积分账本，2026-08-17 双 Server 同时实测均=5645）——任一 Server 查一次即可。

| 工具 | 积分/次 | 档位 | 建议 |
|---|---|---|---|
| ① law_vector_search | **15** | 语义检索 | 自然语言发现法条，贵但值（错别字容错）|
| ② rh_ft_search | **5** | 关键词检索 | **法条关键词检索默认首选，省** |
| ③ rh_fg_search | **5** | 关键词检索 | 法规发现 + 命中条文段预览 |
| ④ rh_fg_detail | **5** | 详情 | 整法全文 + refer_date 历史版本 |
| ⑤ rh_ft_detail | **1** | 详情 | **★全场最便宜！取单条法条必用它** |
| ⑥ case_vector_search | **15** | 语义检索 | 口语化发现案例（完整语义 query 质量极高）|
| ⑦ rh_qwal_search | **5** | 关键词检索 | 权威案例精查（有 Total）|
| ⑧ rh_ptal_search | **5** | 关键词检索 | 普通案例 + **yyft 援引法条反查** |
| ⑨ rh_case_details | **10** | 详情 | 案例完整全文（要引用原文必走）|

**预算纪律（单任务）：**
- 每次批量查询前先 `yuandian_get_user_balance` 确认余额（任一 Server 即可）
- **常见工作流成本估算**：A 法规引用 = ①(15) + ②(5) + ⑤(1) ≈ **21 分**；B 历史版本 = ④(5) 或 ⑤(1)；C 类案研判 = ⑥(15) + ⑦(5) + ⑧(5) + ⑨(10) ≈ **35 分**
- 省分口诀：**能关键词（5分）不语义（15分）；取单条法条用⑤（1分）；检索定位后再下钻详情；避免反复大 top_k/return_num 裸查**

---

## 七、已知服务端缺陷备忘（遇到别浪费时间）

- **⑦ qwal 个别案例源数据截断**：指导案例45号（百度诉青岛奥商案）content 停在"www.job"半截话，多次稳定复现——引用长文前抽查结尾，或走⑨详情核实
- **⑧ ptal content 质量参差**：~20% 缺失/片段（调解书/撤诉裁定 content=null）——要完整全文必走⑨
- **⑧ ptal 数据质量**：ah 字段被案由名填充（无案号文书）；cj 字段偶发异常值"4"
- **① 语义库覆盖不全**：仅索引 10 级中央法条，7 种地方类/工作文件类语义必空（数据存在，走②）
- **② top_k 文档"max50"与服务端不符**：51→51 条软上限；大 top_k 响应体巨大
- **④⑤ fgmc 版本后缀陷阱**：带"(2012修正)"后缀误匹配旧版——按"查版本三原则"操作
- **⑥ wszl 编码与⑦⑧ wszl 名称不一致**——跨工具最易踩的参数坑

---

*本指南对应截止 2026-08-17 元典 MCP（law+case 两 Server）实测情况，如后续元典 MCP 有更新，相关内容以实际更新为准。*
